# Claude Code Cleanup Skills

This repository is a Claude Code plugin-style skill collection.

Use the skills under `skills/` for:

- necessary-code audits
- unused-code audits
- page-flow cleanup audits

Each skill is self-contained and should remain installable through the paths declared in `.claude-plugin/marketplace.json`.
